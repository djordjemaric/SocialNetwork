image: maven:3.9.6-openjdk-17

definitions:
  steps:
    - step: &build
        name: Build the application
        caches:
          - maven
        script:
          - mvn -B clean package -DskipTests --file pom.xml
        artifacts:
          - target/*.jar

pipelines:
  default:
    - step: *build
